show:function() {
  __ITERATE__(<@ this.e @>, <@ function(a) {
    if (("none" == $(a).css('display')) || !a.ownerDocument.documentElement.contains(a)) {
//    if ("none" == a.style.display) {
      a.style.display = <?php if (isFeatureEnabled('toggle') || isFeatureEnabled('hide')): ?>a.__picoquerydata && a.__picoquerydata[1] ? a.__picoquerydata[1] : <?php endif; ?>$("<" + a.nodeName + "></" + a.nodeName + ">").css("display");
    }
  } @>);
  return this;
}
