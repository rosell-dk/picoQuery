/*
hm. doesnt work
remove:function(a) {
  __ITERATE__(<@ this.filter(a||'*') @>, <@ function(b) {
    if (b.parentNode) {
      __CLEAN_DATA__(<@ b @>);
      b.parentNode.removeChild(b);
    }
  } @>);
  return this;
}
*/

remove: function(selector) {
  
  var $sel = this.filter(selector||'*');
  __ITERATE__(<@ $sel.e @>, <@ function(el) {
    if (el.parentNode) {
      __CLEAN_DATA__(<@ el @>);
      el.parentNode.removeChild(el);
    }
  } @>);
  return this;
}

