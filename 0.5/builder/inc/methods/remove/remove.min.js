remove: function(selector) {  
  __ITERATE__(<@ this.filter(selector||'*').e @>, <@ function(el) {
    if (el.parentNode) {
      __CLEAN_DATA__(<@ el @>);
      el.parentNode.removeChild(el);
    }
  } @>);
  return this;
}

