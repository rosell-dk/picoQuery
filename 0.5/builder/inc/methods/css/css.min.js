css:function(a, d) {
  function f(c, b) {
    ({"column-count":1, columns:1, "font-weight":1, "line-height":1, opacity:1, "z-index":1, zoom:1})[a] || "number" != typeof b && ("width" != a && "height" != a || !b.match(/^[\d\.]+$/)) || (b += "px");
    c.style[a] = b + (+(!{"column-count":1, columns:1, "font-weight":1, "line-height":1, opacity:1, "z-index":1, zoom:1}[a] && ("number" == typeof b || ("width" == a || "height" == a) && b.match(/^[\d\.]+$/))) ? "px" : "");
  }
  if (__IS_OBJECT__(<@ a @>)) {
    for (key in a) {
      this.css(key, a[key]);
    }
    return this;
  }
  if (__IS_ARRAY__(<@ a @>)) {
    var g = {}, k = this;
    __ITERATE__(<@ a @>, <@ function(a) {
      g[a] = k.css(a);
    } @>);
    return g;
  }
  if (1 in arguments) {
    __ITERATE__(<@ this.e @>, <@ function(c, b) {
      __IS_FUNCTION__(<@ d @>) ? f(c, d.call(c, b, $(c).css(a))) : f(c, d);
    } @>);

  } else {
    if (!this.e[0]) {
      return;
    }
    var e = getComputedStyle(this.e[0]);
    if ("marginLeft" == a.replace(/^-ms-/, "ms-").replace(/-([\da-z])/gi, function(a, b) {
      return b.toUpperCase();
    })) {
      var e = this.e[0].getBoundingClientRect().left, l = this.e[0].style["margin-left"], h;
      this.e[0].style["margin-left"] = 0;
      h = this.e[0].getBoundingClientRect().left;
      this.e[0].style["margin-left"] = l;
      return e - h + "px";
    }
    return e[a] || this.e[0].style[a];
  }
  return this;
}
